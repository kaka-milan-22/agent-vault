node_modules/
dist/
.agent-vault/
*.tgz

# Native binaries (built by native/macos/build.sh, packed via npm files but
# never committed — keep bin/.gitkeep so the directory exists in the repo).
bin/*
!bin/.gitkeep

# Build / test artifacts
coverage/
*.tsbuildinfo

# Env / secrets
.env
.env.*
!.env.example

# Editors / OS
.DS_Store
.vscode/
.idea/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
